pwd // Print Working Directory

##GIT
-git status // Shows the current status of your project

source venv/bin/activate
Brief :
Activates the virtual environment
1-source = Runs commands from a file in the current shell
2-venv = name of file
3-bin = executables file

Creating the environment = python3 -m venv venv

pip list = Lists all installed packages in the current environment

pip freeze = Lists installed packages in a format suitable for saving