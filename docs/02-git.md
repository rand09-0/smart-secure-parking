git status // Shows the current status of your project

git init // Creates a new Git repository in the current folder 

git add (the file name or . ) exp = git add . // Adds files to the staging area (preper only)
