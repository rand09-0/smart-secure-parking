git status // Shows the current status of your project

1-git init // Creates a new Git repository in the current folder 

2-git add (the file name or . ) exp = git add . // Adds files to the staging area (preper only)

git commit -m "Initial project setup"
git commit = احفظ لقطة للمشروع
-m = i have masseg 
"Initial project setup" = اسم هذه اللقطة

before close vs code :
1-git status
2-git add .
3-git commit -m ""
4-git push